---
title: Подпись запросов
description: HMAC по method, target и digest точных request bytes.
sources:
  - eazy_sdk.request.hmac_sha256
  - eazy_sdk.request.SigningKey
  - eazy_sdk.request.SigningKeyRequirement
  - eazy_sdk.request.body_digest
  - eazy_sdk.request.header_output
api_fingerprint: "sha256:d5b2d146f4a67efeaaaf7f15220db08a5f21a39a7e3cb34b2376c46630fd3969"
---

Создайте подпись из typed компонентов и передайте её в `signing=` операции или в атрибут
`signing` роутера. Ключи приходят из `Identity(key_provider=…)`: алгоритм — контракт API,
ключ — секрет вызывающей стороны. Secret не входит в operation arguments.

```python
from dataclasses import dataclass

from eazy_sdk import AsyncApi, Http, HttpOperation, Identity, JsonField, op
from eazy_sdk.request import (
    SigningKey,
    SigningKeyRequirement,
    body_digest,
    header_output,
    hmac_sha256,
    join,
    method,
    path,
)

PAYMENTS_KEY = SigningKeyRequirement("payments-key")
VENDOR_HMAC = hmac_sha256(
    key=PAYMENTS_KEY,
    base=join(
        method(),
        path(),
        body_digest("sha256"),
        separator=b"\n",
    ),
    output=header_output("X-Signature"),
)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreatePayment(HttpOperation[Payment]):
    __http__ = Http.post("/payments", signing=(VENDOR_HMAC,))

    amount: JsonField[int]

class PaymentsApi(AsyncApi):
    create = op(CreatePayment)
```

Key provider разрешает `SigningKeyRequirement` перед первой отправкой:

```python
def load_signing_key(requirement: SigningKeyRequirement) -> SigningKey:
    if requirement == PAYMENTS_KEY:
        return SigningKey("replace-with-secret")
    raise LookupError(requirement.name)

identity = Identity(key_provider=load_signing_key)
```

Eazy SDK сначала сериализует точные bytes, затем считает digest и добавляет signature header.
Retry и redirect заново выполняют preparation/signing. Пример: request JSON `{"amount":100}` с
headers `Content-Type: application/json` и `X-Signature: ...`; response status `201`, JSON
`{"id":"p-1"}`.

Signer читает immutable prepared request. Он может писать только объявленные outputs. Missing key,
неподдерживаемый exact wire profile или конфликт output завершается до adapter emit. Не записывайте
key и signature base с secrets в observer logs.
