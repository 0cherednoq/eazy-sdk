---
title: Path
description: Подставьте typed значение в URL path.
sources:
  - eazy_sdk.request.Path
api_fingerprint: "sha256:d23df2adf4b0fb285729788cd46d25363fe4500badded62707c91f3f39333d3c"
---

Path parameter заменяет placeholder в URL. Поле операции объявляет его тип, а имя поля —
это имя placeholder'а.

```python
from dataclasses import dataclass

from eazy_sdk import Http, HttpOperation, Path, SyncApi, op

@dataclass(frozen=True, slots=True, kw_only=True)
class GetUser(HttpOperation[User]):
    __http__ = Http.get("/users/{user_id}", operation_id="getUser")

    user_id: Path[int]

class UsersApi(SyncApi):
    get_user = op(GetUser)

user = UsersApi(client).get_user(user_id=42)
```

Вызов отправит `GET /users/42`. Если placeholder называется иначе, чем поле, задайте имя маркером:
`user: Annotated[int, markers.Path("id")]` для `{id}` в шаблоне.

Eazy SDK отклоняет класс роутера при импорте, если placeholder не объявлен полем, `Path`-маркер не
имеет placeholder'а или один placeholder описан несколько раз. Optional path parameters не
поддерживаются: URL всегда должен быть однозначным.

Пример ответа: status `200`, `Content-Type: application/json`, body `{"id":42,"name":"Ada"}`.
