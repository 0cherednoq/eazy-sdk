---
title: Bytes
description: Отправьте готовое бинарное тело.
sources:
  - eazy_sdk.request.BytesBody
api_fingerprint: "sha256:435570d4e766c05a3a7098d3afed40f5ab6dd855e4df4774a184148f34579bae"
---

`BytesBody` отправляет готовое буферизованное тело без JSON/form кодирования.

```python
from dataclasses import dataclass
from typing import Annotated

from eazy_sdk import Http, HttpOperation, Path, SyncApi, op
from eazy_sdk.request import markers

@dataclass(frozen=True, slots=True, kw_only=True)
class PutObject(HttpOperation[Upload]):
    __http__ = Http.put("/objects/{name}", operation_id="putObject")

    name: Path[str]
    payload: Annotated[bytes, markers.BytesBody(content_type="application/octet-stream")]

class ObjectsApi(SyncApi):
    put_object = op(PutObject)

result = ObjectsApi(client).put_object(name="x", payload=b"abc")
```

Вызов отправит три неизменённых bytes и `Content-Type: application/octet-stream`. Для gzip или
deflate укажите `content_encoding`; Eazy SDK сожмёт body до signing. Для повторно создаваемого
stream используйте `ReplayableStreamBody`, иначе unsafe replay будет отклонён.

Пример ответа: status `201`, header `ETag: "42"`, JSON `{"stored":true}`.
