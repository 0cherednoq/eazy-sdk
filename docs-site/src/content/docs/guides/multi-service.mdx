---
title: Мультисервисный SDK
description: Адрес сервиса объявляет роутер, исполнителя выбирает сборка через bind().
sources:
  - eazy_sdk.SyncRoot
  - eazy_sdk.AsyncRoot
  - eazy_sdk.bind
  - eazy_sdk.Binding
  - eazy_sdk.api_group
api_fingerprint: "sha256:209cf9bd29b0764948fcea4236c529b79888a1e1a6b8757a9571e7b74f972184"
---

Адрес сервиса — часть описания API, наравне с path. Роутер объявляет его атрибутом класса, а
общее для нескольких роутеров одного сервиса выносится в обычный базовый класс. Отдельного типа
для «домена» нет: общие атрибуты в Python выражаются наследованием.

```python
from dataclasses import dataclass

from eazy_sdk import Http, HttpOperation, JsonField, Path, SyncApi, SyncRoot, api_group, op

class PaymentsService:
    """Обычный миксин: всё, что общее у роутеров одного сервиса."""

    base_url = "https://pay.shop.com"
    errors = PAYMENT_ERRORS

@dataclass(frozen=True, slots=True, kw_only=True)
class RegisterCard(HttpOperation[Card]):
    __http__ = Http.post("/v1/cards", success={201: Card})

    number: JsonField[str]

@dataclass(frozen=True, slots=True, kw_only=True)
class GetBook(HttpOperation[Book]):
    __http__ = Http.get("/books/{book_id}")

    book_id: Path[int]

class CardApi(PaymentsService, SyncApi):
    register = op(RegisterCard)

class BooksApi(SyncApi):
    get = op(GetBook)

class ShopSdk(SyncRoot):
    books = api_group(BooksApi)   # адрес не объявлен → берётся из клиента
    card = api_group(CardApi)     # адрес из PaymentsService
```

Однохостовый SDK не пишет ни миксина, ни привязок: объявление не длиннее обычного.

## Резолв адреса операции

| Приоритет | Источник |
|---|---|
| 1 | Абсолютный URL в самой декларации операции |
| 2 | `base_url` из `bind(...)` для класса роутера или его базы |
| 3 | `base_url`, объявленный на классе роутера или унаследованный по MRO |
| 4 | `base_url` клиента |

Отсюда главное свойство: мультисервисный SDK работает **на одном клиенте**. Отдельный клиент
нужен только там, где отличаются транспортные политики: другой impersonation/TLS-профиль, другой
proxy, отдельная guard-сессия, другие retry и timeout, другой handler.

## Сборка: `bind()`

`bind()` принимает **класс** — роутер или его миксин. Совпадение ищется по MRO, наиболее
специфичный класс выигрывает.

```python
from eazy_sdk import Client, bind

main = Client.httpx(base_url="https://api.shop.com")

# один клиент на все сервисы: адреса берутся из роутеров
sdk = ShopSdk(main)

# платежам нужен свой impersonation и своя guard-сессия
sdk = ShopSdk(
    main,
    bindings=(bind(CardApi, client=Client.curl_cffi(impersonate="chrome")),),
)

# stage: та же декларация, другой адрес — на весь сервис сразу
sdk = ShopSdk(main, bindings=(bind(PaymentsService, base_url="https://stage-pay.shop.com"),))
```

Один клиент на несколько сервисов означает общую guard/protection-сессию, общий proxy и общий
cookie jar; cookie при этом остаются домен-скоупными по RFC 6265. Если сервису нужна собственная
транспортная политика — дайте ему свой клиент через `bind(..., client=...)`.

## Контракт объявляется один раз

Подпись, шифрование и представление — такие же атрибуты сервиса, как `base_url`: они объявляются
на роутере или на общем миксине и наследуются по MRO. Повторять их в каждом декораторе не нужно.

```python
from dataclasses import dataclass

from eazy_sdk import AsyncApi, Http, HttpOperation, JsonField, op
from eazy_sdk.crypto import encrypt_encoded, encrypt_outbound, payload_crypto
from eazy_sdk.request import JsonPolicy, Wire, header_output, hmac_sha256, method

ROUTER_SIGNATURE = hmac_sha256(
    key=SIGNING_KEY, base=method(), output=header_output("X-Router"), name="router"
)
SERVICE_CRYPTO = payload_crypto(
    "payments", outbound=encrypt_outbound(encoded=encrypt_encoded(using=cipher))
)

class PaymentsService:
    base_url = "https://pay.shop.com"
    security = PAYMENTS_TOKEN
    signing = (ROUTER_SIGNATURE,)
    crypto = SERVICE_CRYPTO
    wire = Wire(encoding=JsonPolicy(ensure_ascii=True))
    errors = PAYMENT_ERRORS
    signed = True          # операция без подписи отвергается при компиляции

@dataclass(frozen=True, slots=True, kw_only=True)
class Charge(HttpOperation[Receipt]):
    """Подпись, crypto и wire — от сервиса."""

    __http__ = Http.post("/charge")

    account: JsonField[str]

@dataclass(frozen=True, slots=True, kw_only=True)
class Refund(HttpOperation[Receipt]):
    """Своя подпись, шифрования нет."""

    __http__ = Http.post("/refund", signing=(REFUND_SIGNATURE,), crypto=None)

    account: JsonField[str]

class PaymentsApi(PaymentsService, AsyncApi):
    charge = op(Charge)
    refund = op(Refund)
```

Объявление в `__http__` не «добавляется» к сервисному, а **заменяет** его целиком: глядя на
операцию, всегда видно, чем именно она подписана. Единственное исключение — `errors` и `wire`,
где складывать безопасно, потому что ни то, ни другое не может тихо ослабить требование сервера.

| Атрибут | Слияние по MRO | На операции |
|---|---|---|
| `base_url` | побеждает ближайший непустой | абсолютный URL в пути сильнее всех |
| `security` | побеждает ближайший объявленный | `security=` заменяет, `security=None` снимает схему |
| `signing` | побеждает ближайший непустой | `signing=` заменяет, `signing=()` снимает подпись |
| `crypto` | побеждает ближайший объявленный | `crypto=` заменяет, `crypto=None` отключает |
| `wire` | `Wire.over(...)` — по полям | по полям поверх сервисного |
| `errors` | складываются | `errors=` добавляются, `inherit_errors=False` отбрасывает сервисные |
| `protocol` | побеждает ближайший объявленный | не объявляется: берётся конвертом `Rpc.method` |
| `allow`, `signed` | ближайший объявленный / логическое «или» | не объявляются |

Снять наследование на подклассе роутера нельзя: пустое значение (`signing = ()`, `security =
None`) читается как «не объявлено» и наследование не отменяет — отказ от сервисного правила
выражается на операции, где он виден.

Что **не** наследуется от сервиса: успешные случаи (они задают тип результата, а он —
часть сигнатуры операции; как убрать повтор, см.
{doc}`Успешный ответ </guides/responses/success>`), `protections`, `requires`/`inject`,
`idempotent`.

## Что объявляет роутер, что клиент

| Уровень | Что несёт | Почему |
|---|---|---|
| Роутер / миксин сервиса | `base_url`, `errors`, `security`, `signing`, `crypto`, `wire`, `signed`, `allow` | контракт API: что сервер требует и что возвращает |
| Клиент | `base_url` по умолчанию, retry, timeout, proxy, impersonation, handler, guard/protection | доставка байтов |

Цепочка дефолтов — одна: корень → MRO роутера → объявление операции. Слияние выполняется один раз
при разрешении дескриптора операции, а не на каждом вызове.

## Ошибки объявления и сборки

| Ситуация | Когда падает |
|---|---|
| Два миксина в MRO объявляют разный `base_url` | при объявлении роутера |
| `base_url` не абсолютный URL | при объявлении роутера |
| Операция несёт схему или подпись вне `allow` | при сборке корня |
| `bind()` на класс, которого нет ни в одном MRO групп корня | при сборке корня |
| Две привязки одинаковой специфичности | при сборке корня |
| `api_group` объявлен на роутере, а не на корне | при объявлении роутера |
| Операция объявлена на классе корня | при объявлении корня |

`allow` — это allowlist схем и подписей сервиса: он не даёт унести чужой токен на чужой хост.
Пока `allow` не объявлен, ограничения нет.

## Владение клиентами

`Root(client)` не владеет клиентом: его закрывает тот, кто создал. `Root.from_handler(...)`
создаёт клиент сам и закрывает его в `close()`/`aclose()`; повторный вызов безопасен. Клиенты,
переданные через `bind(..., client=...)`, корень не закрывает никогда.

## Одна сессия на несколько сервисов

Сессия принадлежит не транспорту, а вызывающей стороне: `identity=` на корне обслуживает все его
роутеры — и когда сервисы делят клиент, и когда у каждого свой (см.
{doc}`Session auth </auth/session>`).

```python
sdk = ShopSdk(main, identity=Identity(auth=(SHOP_SESSION.configure(...),)))
```

`allow` на миксине сервиса ограничивает, какие схемы и подписи вообще можно нести на этот адрес,
поэтому один логин на два сервиса не означает, что токен уедет на третий.
