[project]
name = "eazy_sdk-generated-consumer-fixture"
version = "0.2.0a1"
requires-python = ">=3.13"
dependencies = ["eazy-sdk[pydantic]==0.2.0a1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["consumer_sdk"]
