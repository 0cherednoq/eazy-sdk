[project]
name = "eazy_sdk-generated-consumer-fixture"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["eazy-sdk[pydantic]==0.1.0a1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["consumer_sdk"]
